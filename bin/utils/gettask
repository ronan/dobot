#!/bin/sh

pattern="^( *)- \[.*\] $(quotetask "$DOBOT_TASK")$"
task=$(readtasks | sed -rn "/$pattern/,$ { /$pattern/ { p; b; }; /- \[/q; p; }")

# if [ -z "$task" ]; then err "Could not find $DOBOT_TASK"; fi
echo "$task";